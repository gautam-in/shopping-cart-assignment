{
  "name": "code-exercise",
  "version": "1.0.0",
  "description": "This is a shopping cart coding exercise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "json-server ./server/db.json",
    "start:client": "cd client && export PORT=4000 && npm run start",
    "start": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "install:client": "cd client && npm install",
    "install": "npm run install:client"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "canned": "^0.3.12"
  },
  "devDependencies": {
    "concurrently": "^7.2.1"
  }
}
