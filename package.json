{
  "name": "code-exercise",
  "version": "1.0.0",
  "description": "This is a shopping cart coding exercise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "canned -p 3000 ./server",
    "start:client": "cd client && set PORT=5000 && npm run start",
    "start": "concurrently --kill-others-on-fail \"npm run start:server\" \"npm run start:client\"",
    "install:client": "cd client && npm install",
    "install": "npm run install:client"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "canned": "^0.3.12"
  },
  "devDependencies": {
    "concurrently": "^7.2.1",
    "eslint": "^8.18.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.2",
    "nodemon": "^2.0.16",
    "prettier": "^2.7.1"
  }
}
