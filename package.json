{
  "name": "code-exercise",
  "version": "1.0.0",
  "description": "This is a shopping cart coding exercise",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "node-sass  -rw resources/scss -o resources/css/",
    "compile:sass-dev": "node-sass  -rw resources/scss -o resources/css/ -w",
    "start": "npm run compile:sass > ./out.log | node server.js",
    "start:dev": "npm run compile:sass-dev > ./out.log | nodemon server.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "express-ejs-layouts": "^2.5.0",
    "node-sass": "^4.11.0",
    "nodemon": "^1.18.10"
  }
}
