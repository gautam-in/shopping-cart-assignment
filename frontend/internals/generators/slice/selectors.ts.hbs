import { createSelector } from '@reduxjs/toolkit';

import { RootState } from 'types';
import { {{ properCase sliceName }}State } from './types';

const selectSlice = (state: RootState) => state.{{ camelCase sliceName }} || {{ properCase sliceName }}State;

export const select{{ properCase sliceName }} = createSelector(
  [selectSlice],
  state => state,
);
