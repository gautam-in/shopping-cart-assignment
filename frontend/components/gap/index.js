const Gap = ({ height, className }) => {
  return (
    <div
      style={{
        width: '100%',
        height,
      }}
      className={className}
    />
  );
};

export default Gap;
