export const openRoutes = {
  root: '/',
};

export const errorRoutes = {
  pageNotFound: '/404',
};

export const protectedRoutes = {
  register: '/register',
  signIn: '/signin',
  products: '/products',
};
