module.exports = {
  BASE_URL: "http://localhost:3000",
  TIMEOUT_SEC: 100,
  CARTUPDATE_DELAY: 100,
  MESSAGECATALOG: {
    INVALID_PASSWORD:
      "Password must be of 6 character containing a number and an alphabet",
    INVALID_CNF_PASSWORD: "Value must be same as Password",
  },
};
