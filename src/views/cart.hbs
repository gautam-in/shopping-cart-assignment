<section class="cart-wrapper cart container" id="cart">
    <div class="my-cart">
        <h3 class="font-color">My Cart<span class="font-color">(<i id="item_counter" class="font-color">{{item_counter}}</i> item)</span></h3>
    </div>
    {{!-- <div class="cart__header--close" onclick="common.closeShoppingCart(event)">
            <img src="static/images/cancel.png" alt="close">
        </div>  --}}
    {{#if productInCart}}
    <div class="cart-items">
        {{#each productInCart}}
            <div class="item-wrapper">
                <div class="item-img float-left">
                    <img class="img-cart" src="{{imageURL}}" alt="{{name}}" tabindex="0">
                </div>
                <div class="item-details float-left">
                    <div class="float-left items">
                        <h4>{{name}}</h4>
                        <div class="item-button">
                            <button class="btn btn-minus"
                                onclick="addRemoveItem('{{id}}', {{@index}},
                                {{price}},'minus')">-</button>
                            <input type="text" value="{{count}}"
                                class="prod_quantity" id="prod{{@index}}">
                            <button class="btn btn-plus"
                                onclick="addRemoveItem('{{id}}', {{@index}}, {{price}},'plus')">+</button>
                            <span class="price">Rs.{{price}}</span>
                        </div>
                    </div>
                    <div class="item-total float-right" id="totalp{{@index}}" >
                        Rs. {{total_price}}
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
    {{> components/molecules/m-offer}}
    <div class="checkout">
        <p>Promo code can be applied on payment page</p>
        <a class="btn-checkout" href="/"><span class="font-color float-left">Proceed to checkout</span><span class="font-color float-right " id="totalCheckoutAmount">Rs. 0</span></a>
    </div>
    {{else}}
        <div class="cart-exception">
            No Items in the cart
            <p class="helper-text">You favourite items are just a click away</p>
            <a class="btn-start" href="/product">Start Shopping</a>
        </div>

    {{/if}}
</section>