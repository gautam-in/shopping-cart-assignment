export const selectCategories = (state) => state.categories.categories;

export const isLoadingSelect = (state) => state.categories.isLoading;

export const selectActiveCategory = (state) => state.categories.activeCategory;
