export const usersSelector = state => state.users;

export const cartSelector = state => state.cart;

export const bannersSelector = state => state.banners;

export const categoriesSelector = state => state.categories;

export const productsSelector = state => state.products;

export const userLoginStatusSelector = state => state.isUserLoggedIn;

export const alertSelector = state => state.alert;

export const categoryFilterSelector = state => state.categoryFilter;

export const cartModalViewSelector = state => state.showCartModalView;