import { CommonBtnDirective } from './common-btn.directive';

describe('CommonBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonBtnDirective();
    expect(directive).toBeTruthy();
  });
});
