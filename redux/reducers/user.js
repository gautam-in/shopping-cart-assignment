import * as t from '../types'

const defaultState = {
    name:'guest',
    cart:[]
}

const user = (defaultState,action)=>{
    switch(actipn.type){
        case t.SET_USER:
            return action.payload
            break;
        default:
            return 
        break;
    }
}