import styled from 'styled-components';

export const ImageConatiner = styled.div`
  max-width: var(--maxWidth);
  margin-top: 2rem;
  .slick-next::before,
  .slick-prev::before {
    color: black;
  }
`;
