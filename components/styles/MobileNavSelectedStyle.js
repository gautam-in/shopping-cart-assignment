import styled from "styled-components";

const MobileNavSelectedStyle = styled.div`
  text-align: center;
  padding: 0.5rem;
  background-color: var(--magenta);
  color: var(--white);
  .chevron-icon {
    float: right;
    transition: 0.2s;
  }
`;
export default MobileNavSelectedStyle;
