{{>header cartTotal=cartTotal cartData=cartData}}
<main class="container d-flex flex-wrap flex-autoheight clearSpace">
    <aside class="category">
        <ul>
            {{#each categories}}
            <li><a href="/products/{{key}}">{{name}}</a></li>
            {{/each}}
        </ul>
    </aside>
    <div class="productList">
        {{#each products}}
        <div class="productList-item">

            <div class="w-100">
                <h3>{{name}}</h3>
            </div>
            <div class="productList-media">
                <img class="fluid-image" alt="{{name}}" src={{imageURL}} />
            </div>
            <div class="productList-tool">
                <div class="productList-description font-12 greybg">{{description}}</div>
                <a class="productList-mobileAdd btn btn-red" href="/products/cart/{{sku}}">By now @
                    {{price}}</a>
            </div>
        </div>
        {{else}}
        <div class="w-100">
            <h3 class="text-center"> No Products found</h3>
        </div>
        {{/each}}
    </div>
</main>