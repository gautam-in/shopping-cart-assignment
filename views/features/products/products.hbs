<section class="parent-ctr">
    <div class="category-wrapper">
   			<ul class="md-screen">
				{{#each categories}}
				<li class="category-bg padding10 {{this.id}}" onclick="selectCategory('{{this.id}}')">
					<span>{{this.name}}</span>
				</li>	
				{{/each}}
			</ul>
			<div class="sm-screen padding5">
				<div class="dropdown">
		    		<button class="dropbtn">Select Categories 
				      <i class="fa fa-caret-down"></i>
				    </button>
				    <div class="dropdown-content">
				    {{#each categories}}
				     <a onclick="selectCategory('{{this.id}}')">{{this.name}}</a>
					{{/each}}
			    </div>
			  </div> 
			</div>
	</div>
    <div class="product-wrapper">
        {{#each products}}
        {{> molecules/m-product this}}
        {{/each}}
    </div>
</section>